# 个人生活工作台 · 产品深度规划 V2

## 0. 产品定义

### 一句话定位

**一个单用户、多端同步、Agent Native 的个人生活数据工作台：人和授权 AI Agent 都可以记录事实、读取状态、执行操作、提交建议，并围绕长期生活数据形成持续反馈。**

它不是传统 Dashboard，也不是聊天机器人，更不是“生活管理模块大全”。

产品真正形成闭环的是：

```text
记录事实
→ 形成个人 Context
→ Agent / 规则理解
→ 给出建议
→ 用户决策与执行
→ 产生新数据
→ 长期复盘
```

原需求中“作息锚点是立身之本”的定位不变。三个锚点仍然承担第一阶段留存验证，而不是因为增加 Agent 能力就降低优先级。

---

# 1. 产品模块规划

## 1.1 P0：作息锚点

核心：

- 06:50 起床
- 健身结束距睡眠 ≥3h
- 22:15 关灯
- 当日完成状态
- 连续天数
- 30 天热力图
- 实际时间与目标偏差

核心字段：

```text
date
anchor_type
target_at
actual_at
status
note
source
updated_at
version
```

**结论：绝对保留。**

理由：这是目前唯一具有明确每日频次、目标、反馈周期和真实使用动机的功能，是判断整个产品是否成立的核心指标。

---

## 1.2 P0：今日

今日不是 Dashboard，而是**行动页面**。

只回答四件事：

1. 今天是什么日型
2. 三个锚点完成到哪里
3. 今天还有什么必须做
4. 是否存在重要建议或异常

信息结构：

```text
今日 · 健身工作日

06:50 起床        ✓ 06:47
19:30 健身        待完成
22:15 关灯        —

下一步
19:30 前开始训练

建议
今晚不建议安排 21:00 后游戏
```

禁止卡片海、无意义图标、评分仪表盘。继续执行报告要求的极简、高留白、强层级。

### 视觉验收标准（新增：V2.1）

今日页采用**纵向单主轴布局**，顺序固定：

```text
日期 + 日型
↓
锚点区
↓
下一步
↓
建议
```

锚点区是页面绝对视觉主体。每行使用三列稳定对齐：

```text
06:50    起床             已完成 06:47
19:30    健身             待完成
22:15    关灯             —
```

**左列时间**使用等宽数字或稳定数字宽度，低强调；**中列名称**承担主要阅读权重；**右列状态**右对齐，仅完成/异常状态使用必要强调，不使用大面积色块。

三个锚点之间通过留白和细分隔关系建立节奏，不包装成三个独立卡片。

“下一步”置于锚点下方，字号与权重低一级，只突出一个当前行动。

“建议”权重再次降低，默认最多显示 1–2 条，仅展示标题与一句依据；不得在视觉上超过锚点区，也不得形成 AI 卡片瀑布。

开发验收标准不是“看起来简洁”，而是：

> **进入今日页 2 秒内，视线首先落在三个锚点；其次知道下一步；最后才注意到 Agent 建议。**

---

## 1.3 P0.5：Inbox

提供唯一的低摩擦入口：

> **记一下……**

允许输入未结构化信息：

- 周末买洗衣液
- 明天不玩游戏
- 今天 7:10 才起来
- 周末研究 NAS
- 想去杭州
- 明天健身

后续由用户或 Agent 分类到：

```text
待办
待买
锚点
健身
日程
备忘
目标
仅存档
```

**结论：强制加入核心路线。**

理由：如果所有信息都要求先进入正确模块，输入成本会迅速升高；Inbox 是人和 Agent 共同使用工作台的统一入口。

---

## 1.4 P0.5：日型

建立正式的 `day_type`：

```text
普通工作日
健身工作日
休息工作日
周末
周末健身日
出差日
旅行日
自定义
```

日型包含：

```text
name
default_anchors
default_tasks
allowed_game_window
workout_expected
sleep_target
```

**结论：保留并提前。**

理由：Agent 要回答“今天应该怎么安排”，首先需要知道今天是什么类型，而不是每次重新推理。

---

## 1.5 P1：习惯

支持三种：

```text
check
count
value
```

例如：

- 护肤 ✓
- 喝水 5 次
- 体重 78.5kg

复用锚点的连续天数与热力图组件。

**保留，但不得进入第一版 MVP。**

---

## 1.6 P1：健身

只做：

```text
date
type
duration
completed
note
weight?
```

不做动作库、组数模板、课程系统。

**保留。**

价值是让工作台理解“是否完成每周 4 次训练”，而不是替代专业健身 App。

---

## 1.7 P1：每周复盘

这是锚点之后第二重要的数据消费能力。

每周自动生成：

```text
起床达标 5/7
关灯达标 4/7
健身 3/4

与上周变化

最明显问题
周二 / 周四容易晚睡

Agent 建议
下周这两天取消 21:00 后游戏
```

复盘同时可以被 Codex、ChatGPT、Claude 等读取。

**结论：必须做。**

因为单纯记录最终会失去动力，复盘负责证明这些记录是否产生价值。

---

## 1.8 P1：统一时间线

记录人与 Agent 的所有重要事件：

```text
06:47 human   anchor.completed
12:32 codex   shopping.created
19:11 human   workout.completed
20:14 claude  advice.created
20:30 human   advice.dismissed
```

用途：

- 回顾生活
- Agent 审计
- 数据错误追踪
- Ask My Life 的事件数据源

**Agent 可写之后必须存在。**

---

## 1.9 P1：Agent Advice Inbox

任何外部 Agent 都可以向工作台提交建议。

建议必须具备：

```text
agent
title
content
reason
evidence
priority
status
expires_at
```

建议不能直接混入事实数据。

状态：

```text
unread
read
accepted
dismissed
expired
```

---

## 1.10 P1：规则与自动化

确定性逻辑交给 Rule，而不是 LLM。

例如：

```text
连续 3 天 >22:30 关灯
→ 触发一次作息提醒

健身日 19:30 仍无训练
→ 手机提醒

训练已完成
→ 当日晚间健身提醒自动取消
```

AI 负责：

> 为什么最近睡晚了？

规则系统负责：

> 连续三天晚睡后提醒。

两者严格分离。

---

## 1.11 P2

保留：

- 待买
- 长期目标
- Ask My Life
- 日历接入
- 数据导入/导出

延期：

- 记账
- Health 数据
- Location Context

砍掉：

- 书影音收藏
- 泛减脂模块

原报告对这些模块本身就没有明确真实需求。

---

# 2. 数据模型

## 2.1 不采用“一行巨型 JSONB”

也不采用每个小模块一张业务表。

推荐：

> **通用业务数据单表 + 基础设施专表。**

### `life_data`

```text
id UUID

user_id
module
entity_key
biz_date

payload JSONB

version
source_type
source_id

created_at
updated_at
deleted_at
```

唯一约束：

```text
(user_id, module, entity_key)
```

适合：

```text
anchor
habit_definition
habit_log
workout
shopping
day_type
goal
inbox
```

理由：

- 单用户数据规模极小
- 新模块增加速度快
- JSONB 对个人产品足够灵活
- 保持当前“低工程复杂度”要求
- 不产生大量 migration

报告原本就倾向单表 JSONB，这一方向保留，但将写入原子由“整个人的数据”缩小到“单个业务记录”。

---

## 2.2 基础设施独立表

以下内容生命周期不同，不应该塞进 `life_data`：

### `agent_clients`

```text
id
name
type
scopes
enabled
created_at
last_seen_at
```

### `agent_advice`

```text
id
agent_id
topic
title
content
evidence JSONB
priority
status
created_at
expires_at
```

### `activity_log`

```text
id
actor_type
actor_id
action
resource
resource_id
metadata
created_at
```

### `automation_rules`

存确定性自动化规则。

---

## 2.3 版本原子性（已改：V2.1）

`life_data.version` 定义为 `BIGINT NOT NULL DEFAULT 1`。

版本自增明确采用 **方案 A：Postgres 数据库触发器**。每次有效 `UPDATE` 时，由数据库原子执行：

```text
BEFORE UPDATE:
NEW.version = OLD.version + 1
```

不允许客户端自行计算 `version + 1`。原因是 Web、App、Codex 等多个客户端可能同时写入，客户端 read-modify-write 存在两个请求读取相同旧版本后分别写入的竞态。

Agent 更新必须同时携带 `expected_version`，业务写入条件等价于：

```text
更新 resource
WHERE id = target_id
AND version = expected_version
```

命中 1 行 → 更新成功，触发器自动递增版本。

命中 0 行 → 返回 `409 Conflict`，Agent 必须重新读取后再决定是否修改。

因此形成两层保障：

**数据库触发器负责“版本一定原子递增”；Application Core 负责“旧 Context 不允许覆盖新数据”。**

---

# 3. 同步与冲突模型

## 人类客户端

采用：

> **记录级 Last Write Wins**

而不是字段级 merge。

例如：

```text
wake:2026-09-15
lights_off:2026-09-15
```

是两个独立记录。

公司机改起床、家机改睡觉：

**互不覆盖。**

两台机器同时修改同一个起床记录：

**后写覆盖。**

单用户情况下足够，而且与原需求判断一致。

---

## Agent

Agent 写操作增加：

```text
expected_version
idempotency_key
```

（V2.1 修订：Agent/API 客户端采用 **Optimistic Concurrency：`expected_version + idempotency_key`**，见 2.3 版本原子性。）

若 Agent 基于 version 7 修改，而当前已经 version 8：

```text
409 Conflict
```

重新读取后才能再次操作。

原因：

> 人自己覆盖自己的修改可以接受；AI 基于过期 Context 覆盖用户真实记录不能接受。

（V2.1 修订补充：所有端均禁止整份 JSON Context 回写，只允许修改目标业务记录，从根源缩小冲突范围。人类客户端继续采用**原子记录级 LWW**。）

---

# 4. Agent Native 架构

## 4.1 定义

所谓多 Agent，不是在产品内部运行很多 Agent。

而是：

> **任何被授权的 Agent 都能把生活工作台当成 Tool 使用。**

包括：

- Codex
- Claude Code
- ChatGPT
- Trae
- Hermes
- 本地 Agent
- 未来其他工具

---

## 4.2 双协议

### MCP

优先服务支持 MCP 的 Agent。

### HTTP API

作为通用兼容层。

两者共用同一个 Application Service。

```text
               Application Core

            MCP             HTTP
             │               │
     Codex / Claude      Other Agents
```

Agent 不直接操作数据库。

---

## 4.3 Tool 必须表达业务语义

禁止只暴露：

```text
query_database()
update_json()
```

应该是：

```text
get_today_context

get_anchor_status
check_anchor

get_habit_progress
record_habit

record_workout

get_inbox
capture_inbox

get_shopping_list
add_shopping_item

get_weekly_summary

submit_advice
```

这样数据库结构变化不会破坏 Agent。

---

# 5. Agent 权限

每个 Agent 独立身份、独立 Token、独立 Scope。

例如：

```text
Codex
read:*
write:anchor
write:habit
write:workout
write:shopping
advice:create
```

Claude：

```text
read:*
advice:create
```

绝不允许：

- 共享人的浏览器 Session
- 暴露邮箱密码
- 暴露 Supabase service_role key

Agent 所有操作进入 `activity_log`。

---

# 6. Web / PWA / App

## 第一阶段：Web

保持报告要求：

- 静态优先
- 无大型构建链
- GitHub Pages
- Supabase

报告实测确认 Supabase 裸连可用，而 GitHub Pages 家机仍需验证。

GitHub Pages 不通：

```text
GitHub Pages
↓
本地 HTML
↓
仍直接连接 Supabase
```

代理作为备选。

不选 Vercel / Workers，因为报告已实测不可用。

---

## 第二阶段：PWA

增加：

- manifest
- 主屏图标
- standalone
- 静态资源离线壳

暂时不做复杂离线数据同步。

---

## 第三阶段：Mobile App

用 Web Core 包装原生客户端。

目标：

```text
iOS
Android
```

App 真正增加的价值不是“套壳”，而是：

- 通知
- 快捷打卡
- 系统分享入口
- 后台能力
- Widget
- 语音快速记录
- Health 数据

业务逻辑、Supabase 和 Agent API 仍只有一套。

---

## 6.4 Supabase 免费层防休眠（新增：V2.1）

Free Plan 项目存在 7 天低活跃后被自动暂停的风险。官方说明低查询量项目可能暂停，且“过去一周每天少量用户数据库请求”通常足以维持活跃，因此不能把防休眠仅依赖人工访问。

### 方案 A｜主方案：Agent Keepalive

Phase 0 至少注册一个常驻 `agent_client`：

```text
Hermes Keepalive
或
Codex Scheduled Agent
```

调用只读健康接口：

```text
GET /health
或
get_system_status()
```

需求方最低门槛：**每周 ≥3 次**。

实际调度建议提高为**每日 1 次**，因为 Supabase 官方并未承诺“每周 3 次请求一定不会暂停”，只说明足够的近期数据库活动可以避免暂停。Keepalive 必须真实触发数据库只读查询，不能只访问静态页面。

### 方案 B｜人工恢复

部署文档记录恢复流程：

**Supabase Dashboard → 选择 Organization → 选择已暂停 Project → `Resume project` → Confirm。**

当前官方恢复入口如此；不把 `Settings → General` 写死，以免 Dashboard UI 调整导致文档失效。

---

# 7. 信息架构

桌面端建议只有：

```text
今日
时间线
习惯
健身
Inbox
更多
```

手机底部：

```text
今日
记录
Inbox
我的
```

全局永久保留：

> `＋ 记一下`

不要随着功能增加不断增加导航入口。

---

# 8. 分期路线

## Phase 0｜基础设施

完成：

- Supabase 项目
- Auth
- RLS
- Schema
- version
- actor/source
- activity log
- Agent Client / Scope 模型
- GitHub Pages 家机实测
- 本地 HTML fallback

### DoD

- 未授权用户无法读取数据
- 公司机与家机存在稳定访问方式
- 所有写入可知道 actor
- Supabase 失败不会白屏
- Agent 数据协议冻结 V1
- （V2.1 新增）至少一个常驻 Agent 已注册到 `agent_clients`
- （V2.1 新增）Keepalive 任务实际运行并留下 `activity_log`
- （V2.1 新增）最低频率 ≥3 次/周，推荐每日 1 次
- （V2.1 新增）部署文档包含 Supabase 暂停识别与人工 `Resume project` 流程
- （V2.1 新增）version 数据库触发器已验证：并发更新不会产生重复版本

---

# MVP｜证明用户真的会用

只上线：

- 锚点
- 今日
- 热力图
- 日型基础
- 同步三态
- 手机响应式

同步状态：

```text
已同步
同步中
同步失败 · 重试
```

### DoD

- A 机记录，B 机刷新可见
- 断网后不静默丢数据
- 30 天热力图正确
- 手机无横向滚动
- 连续实际使用 ≥7 天

与原报告 MVP 验收目标一致。

（V2.1 新增）数据主权验收项——设置页提供：

> **导出全部数据（JSON）**

导出范围包括当前用户拥有的：

```text
life_data
agent_advice
activity_log
automation_rules
agent_clients 的非敏感配置
```

不得导出 Token、密钥、Session 等凭据。

文件格式：

```text
life-workspace_2026-09-15_schema-v1.json
```

JSON 顶层必须包含：

```text
schema_version
exported_at
data
```

由客户端直接读取 Supabase 当前用户可访问数据并生成本地文件，不依赖额外服务器。

理由：终局是 Personal Context Server，个人 Context 必须具备**可迁移、可备份、可脱离供应商恢复**的能力。

---

## 全 Phase 推进门禁（新增：V2.1）

从 MVP → Phase 1 开始，所有阶段统一执行：

> **上一阶段连续真实使用 ≥14 天，且锚点记录率 ≥80%，并由需求方本人确认阶段成立。**

计算：

```text
锚点记录率 =
实际完成记录数 / 当期计划锚点数
```

任一阶段自身 DoD 或上述使用门禁未达成：

**下一 Phase 不启动，没有例外。**

---

# Phase 1｜形成 Context

加入：

- Inbox
- 完整日型
- 习惯
- 健身
- 时间线
- 每周复盘

### DoD

能够回答：

> 我今天应该做什么？

以及：

> 最近一周我执行得怎么样？

---

# Phase 2｜Agent Native

开放：

- MCP
- HTTP Agent API
- Agent Token
- Scope
- Advice Inbox

首先接：

> Codex

再接第二个不同 Agent 验证协议通用性。

### DoD

Codex 可以：

```text
读取今天状态
记录一次生活事件
增加待买
读取最近记录
提交建议
```

且无需理解数据库 Schema。

第二个 Agent 无需修改业务数据结构即可接入。

---

# Phase 3｜主动系统

加入：

- Rule Engine
- 通知
- PWA
- 手机 App
- Agent 建议主动触达

形成：

```text
数据
→ Rule / Agent
→ 通知
→ 操作
→ 新数据
```

---

# Phase 4｜Personal Context Server

后续再逐步接：

- Calendar
- Health
- 长期目标
- Location Context
- 其他个人数据源

此时生活工作台开始成为：

> **用户自己的长期 Context Server。**

---

# 9. 三个核心产品指标

不建议首先看 DAU。

### ① 锚点记录率

```text
实际记录锚点
/
计划锚点
```

这是 MVP 第一指标。

### ② 主动打开率

用户是否在没有提醒的情况下打开工作台。

说明系统本身产生价值。

### ③ Agent 有效操作率

```text
被接受或产生实际价值的 Agent 操作
/
Agent 总操作
```

避免 Agent 接入最后只是技术玩具。

---

# 10. 最大风险

## 风险一：变成“功能收藏项目”

最容易死亡。

今天记账，明天书影音，后天饮食，再做十几个模块。

### 规避

**任何新模块必须来自真实重复行为。**

连续 7 天没有真实需求，不开发。

---

## 风险二：AI 能力比基础数据先膨胀

最后花大量时间研究 Agent，却没有足够生活数据。

### 规避

顺序必须：

```text
记录
→ Context
→ 分析
→ Agent
```

不能反过来。

---

## 风险三：Agent 能写之后破坏真实数据

这是 Agent Native 最大技术风险。

### 规避

必须同时存在：

```text
独立 Token
Scopes
Version Check
Idempotency
Activity Log
```

没有这些条件之前，只开放 Read + Advice。

---

# 11. 最终产品边界

最终不追求：

> 一个什么都能管理的个人 Super App。

而追求三个能力：

### ① Capture

任何设备、任何 Agent 都能快速记录真实生活。

### ② Context

系统长期知道：

> 今天是什么状态、最近发生过什么、目标是什么。

### ③ Action

人和授权 Agent 可以基于 Context：

> 查询、判断、建议、执行。

最终产品可以概括为：

> **Personal Context + Action System**

Web、手机 App 是人的入口。

MCP / HTTP 是 Agent 的入口。

Supabase 是事实底座。

时间线是历史。

Inbox 是输入口。

锚点是第一阶段真正需要证明的使用价值。

如果锚点不能让产品连续使用，就停止扩展；如果能够稳定使用，再逐渐把它发展成真正属于个人的 Agent Native Life OS。