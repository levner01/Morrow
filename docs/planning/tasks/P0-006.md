# P0-006｜建立可降级静态登录壳和同源单HTML发行

状态：PLANNED（本轮仅交付任务说明，未施工）。

## 本卡最低上下文

项目目录：`/Users/wangxinkai/Documents/Morrow`。M1=Phase0+MVP；产品为单用户、多端同步、以三个作息锚点验证留存的静态生活工作台。后端固定Supabase，Human记录级LWW，Agent OCC，DB trigger原子version，所有写可审计；Agent不直连数据库。唯一业务Core在私有PostgreSQL语义函数，Web直接Human RPC，Edge仅Agent适配；M1业务Agent入口关闭，唯一health例外。前端原生HTML/CSS/JS，发行单HTML，默认GitHub Pages+本地fallback。不能引入Phase1功能。

Codex已冻结架构，在M1前不介入；不要把“等待Codex”作为工序。只读本卡指定章节与前置产物即可开工。若与原产品冲突，以原产品V2优先并记录具体冲突；未验证能力标待验证。任何当前合同变更须记录AD补充和独立Review，不能改掉安全或阶段门禁。

## 目标

建立可降级静态登录壳和同源单HTML发行。

## 前置依赖

P0-005已独立Review为PASS；其证据必须实际可读。

## 输入

01-architecture.md AD02/§2/4/8/9；02-contracts.md C05/07；已通过的Auth/RPC边界。 所有相对路径均相对docs/planning，源代码路径相对项目根。

## 施工范围

实现原生HTML/CSS/classic JS分层；前端transport封装SDK、owner登录/refresh/logout、失败面板与配置检查。建立固定SDK自托管和最小按清单内联打包脚本，发行物无运行时CDN依赖，静态源和dist来自同一代码。建立storage probe及失败说明、缓存旧快照标识，不先完成生活UI。公共项目URL/key可配置，密钥不入代码。

## 交付物

index.html、assets/{js,css,vendor}、scripts/package-html与最小测试；dist单HTML+SHA256+release manifest；license与SDK版本记录。

## 明确不做什么

React/大型构建链、今日全功能、MCP、PWA、原生壳。

## 验收条件

- [ ] 普通静态服务能打开；dist HTML能双击加载壳与全部资源；没有远程font/SDK依赖。
- [ ] 错误URL、断网、Auth过期都显示可理解错误和重试，不白屏。
- [ ] 密码/session不进日志/export/business store；logout有未同步草稿处理说明。
- [ ] 打包可重复生成同源行为（排除明确时间字段后hash可复现）；禁止手改dist。
- [ ] fileorigin登录、storage能力留测试结果，最终双机在P0-008验收。

## 推荐执行环境 / 模型

Trae + Kimi K2.7 Code。凭据只在宿主安全存储，模型可用性如未验证先检查。

## 推荐 Review 环境 / 模型

WorkBuddy + DeepSeek V4.1 Flash；Hy4检查失败页面。审查者拿diff、原始验证输出和本卡，不接受作者口头自证。高风险不可只做廉价初审。

## 风险

**Medium**。失败写证据并保持BLOCKED；修复在本任务闭环，不跳至下一任务。

## 完成回执与下一步

填写`docs/planning/evidence/P0-006/result.md`：commit、变更文件、测试实际命令/原始输出、未验证项、Review人/模型/结论、回滚影响。更新task-index.json对应status前先有签核；格式按03-execution.md §3。

完成后才能进入：**P0-007**。
