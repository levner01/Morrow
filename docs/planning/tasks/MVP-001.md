# MVP-001｜实现锚点、日型计划及统一业务语义

状态：PLANNED（本轮仅交付任务说明，未施工）。

## 本卡最低上下文

项目目录：`/Users/wangxinkai/Documents/Morrow`。M1=Phase0+MVP；产品为单用户、多端同步、以三个作息锚点验证留存的静态生活工作台。后端固定Supabase，Human记录级LWW，Agent OCC，DB trigger原子version，所有写可审计；Agent不直连数据库。唯一业务Core在私有PostgreSQL语义函数，Web直接Human RPC，Edge仅Agent适配；M1业务Agent入口关闭，唯一health例外。前端原生HTML/CSS/JS，发行单HTML，默认GitHub Pages+本地fallback。不能引入Phase1功能。

Codex已冻结架构，在M1前不介入；不要把“等待Codex”作为工序。只读本卡指定章节与前置产物即可开工。若与原产品冲突，以原产品V2优先并记录具体冲突；未验证能力标待验证。任何当前合同变更须记录AD补充和独立Review，不能改掉安全或阶段门禁。

## 目标

实现锚点、日型计划及统一业务语义。

## 前置依赖

P0-009已独立Review为PASS；其证据必须实际可读。

## 输入

02-contracts.md C01–05、F01–F14；01-architecture.md §5；P0-009基线。 所有相对路径均相对docs/planning，源代码路径相对项目根。

## 施工范围

在现有Core实现initialize_workspace/set_day_type/check_anchor/clear_anchor/update_anchor_note、get_today_context与计划解析；明确实现record_open_v1（Human owner、按owner/device_id/工作区日期去重，device_id只作去重标识不能作身份凭据），供MVP-002接入；不让Web计算规则。按用户首次选择周训练日建立历史默认计划，无访问日期也有计划分母。首记物化快照，已记录当日允许单向加训练，不能减少分母或改历史目标。训练结束目标19:15，实际睡眠间隔独立评估；次日关灯归属前日。实现只读Context一致锁保护（FOR SHARE；整条函数链VOLATILE，POST RPC而非get:true），MVP初始投影返回下一步。

## 交付物

业务Core migrations；正式命令/响应schema及fixture；所有日期/计划/状态测试；API映射文档。

## 明确不做什么

完整日型模板编辑、习惯/Inbox/健身模块、Agent业务端点、AI建议。

## 验收条件

- [ ] F01–F08/F10/F11/F13/F14通过，真实SQL校验而非仅前端样例。
- [ ] 起床后临时训练能追加当日计划并分母+1；已有训练不能通过改型抹分母。
- [ ] clear只清实际值不删计划；更改未来默认不能重写历史；未打开日期可算分母。
- [ ] Human只提交允许业务字段，actor/version/target由Core掌管；请求与审计原子。
- [ ] record_open_v1同device同日即使更换幂等key也最多1个human.opened；不同日可新建；错误owner拒绝。
- [ ] Context在并发计划/打卡变更中不返回混合快照；旧客户端未知payload安全拒写。

## 推荐执行环境 / 模型

Trae + Kimi K3。凭据只在宿主安全存储，模型可用性如未验证先检查。

## 推荐 Review 环境 / 模型

Hermes + GLM-5.3；WorkBuddy + DeepSeek V4.1 Flash 复核样例。审查者拿diff、原始验证输出和本卡，不接受作者口头自证。高风险不可只做廉价初审。

## 风险

**High**。失败写证据并保持BLOCKED；修复在本任务闭环，不跳至下一任务。

## 完成回执与下一步

填写`docs/planning/evidence/MVP-001/result.md`：commit、变更文件、测试实际命令/原始输出、未验证项、Review人/模型/结论、回滚影响。更新task-index.json对应status前先有签核；格式按03-execution.md §3。

完成后才能进入：**MVP-002**。
