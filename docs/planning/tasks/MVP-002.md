# MVP-002｜交付三锚点主导的今日行动页面

状态：PLANNED（本轮仅交付任务说明，未施工）。

## 本卡最低上下文

项目目录：`/Users/wangxinkai/Documents/Morrow`。M1=Phase0+MVP；产品为单用户、多端同步、以三个作息锚点验证留存的静态生活工作台。后端固定Supabase，Human记录级LWW，Agent OCC，DB trigger原子version，所有写可审计；Agent不直连数据库。唯一业务Core在私有PostgreSQL语义函数，Web直接Human RPC，Edge仅Agent适配；M1业务Agent入口关闭，唯一health例外。前端原生HTML/CSS/JS，发行单HTML，默认GitHub Pages+本地fallback。不能引入Phase1功能。

Codex已冻结架构，在M1前不介入；不要把“等待Codex”作为工序。只读本卡指定章节与前置产物即可开工。若与原产品冲突，以原产品V2优先并记录具体冲突；未验证能力标待验证。任何当前合同变更须记录AD补充和独立Review，不能改掉安全或阶段门禁。

## 目标

交付三锚点主导的今日行动页面。

## 前置依赖

MVP-001已独立Review为PASS；其证据必须实际可读。

## 输入

01-architecture.md §8；02-contracts.md C02/03/05；MVP-001的真实投影/API。 所有相对路径均相对docs/planning，源代码路径相对项目根。

## 施工范围

实现DateDayType、AnchorList/Row、时间修正/clear、下一步、初始周设置、当日日型选择和设置入口。保持纵向单主轴与三列对齐，训练写“健身结束”，n/a弱化但保留第三行；当日起床后追加训练有清楚确认。用transport调用RPC，有限optimistic只显示pending，不自算达标或连续。无建议时隐藏区域。加入record_open辅助真实使用证据。

## 交付物

assets/js/ui与对应CSS；可运行今日页；移动/桌面截图；可访问性操作记录。

## 明确不做什么

大Dashboard、图标装饰堆砌、热力图完整逻辑（MVP-004）、复杂同步（MVP-003）。

## 验收条件

- [ ] 2秒注意力人工观察首先三锚点，其次唯一下一步；无卡片海/虚构AI建议。
- [ ] 打卡/改实际时间/清空/改日型都真实持久化，刷新显示正确，错误有反馈。
- [ ] 320/375/390/768/1280宽无横向溢出；200%文本缩放、键盘、focus与触控区域可用。
- [ ] 训练19:15目标，实际时间与目标状态分离，非训练日不伪造完成。
- [ ] 初始训练日是显式配置，不偷偷推断四天；导航无未来空页面。

## 推荐执行环境 / 模型

Trae + Kimi K2.7 Code。凭据只在宿主安全存储，模型可用性如未验证先检查。

## 推荐 Review 环境 / 模型

WorkBuddy + Hy4（视觉）；Hermes + GLM-5.3（业务）。审查者拿diff、原始验证输出和本卡，不接受作者口头自证。高风险不可只做廉价初审。

## 风险

**Medium**。失败写证据并保持BLOCKED；修复在本任务闭环，不跳至下一任务。

## 完成回执与下一步

填写`docs/planning/evidence/MVP-002/result.md`：commit、变更文件、测试实际命令/原始输出、未验证项、Review人/模型/结论、回滚影响。更新task-index.json对应status前先有签核；格式按03-execution.md §3。

完成后才能进入：**MVP-003**。
