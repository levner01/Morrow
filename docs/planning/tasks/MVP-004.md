# MVP-004｜完成30天热力图与不失真的记录统计

状态：PLANNED（本轮仅交付任务说明，未施工）。

## 本卡最低上下文

项目目录：`/Users/wangxinkai/Documents/Morrow`。M1=Phase0+MVP；产品为单用户、多端同步、以三个作息锚点验证留存的静态生活工作台。后端固定Supabase，Human记录级LWW，Agent OCC，DB trigger原子version，所有写可审计；Agent不直连数据库。唯一业务Core在私有PostgreSQL语义函数，Web直接Human RPC，Edge仅Agent适配；M1业务Agent入口关闭，唯一health例外。前端原生HTML/CSS/JS，发行单HTML，默认GitHub Pages+本地fallback。不能引入Phase1功能。

Codex已冻结架构，在M1前不介入；不要把“等待Codex”作为工序。只读本卡指定章节与前置产物即可开工。若与原产品冲突，以原产品V2优先并记录具体冲突；未验证能力标待验证。任何当前合同变更须记录AD补充和独立Review，不能改掉安全或阶段门禁。

## 目标

完成30天热力图与不失真的记录统计。

## 前置依赖

MVP-003已独立Review为PASS；其证据必须实际可读。

## 输入

02-contracts.md C03全表及F09/F10/F12；MVP-001权威Core与MVP-003状态。 所有相对路径均相对docs/planning，源代码路径相对项目根。

## 施工范围

Core实现30天逐日投影、记录率/目标达标数/实际间隔独立统计、准确连续（不能只查30天）。处理未开始日、未访问日、非训练日、provisional结算、补记和历史变更重算。UI实现低权重热力图和连续说明，文本可访问；图表不反抢锚点注意力。不新增泛数据看板。

## 交付物

统计SQL函数/migration与fixtures；Heatmap30组件；边界测试和截图。

## 明确不做什么

每周复盘、习惯统计、评分/排名/圆环Dashboard；按30天窗口冒充终身连续。

## 验收条件

- [ ] 45天连续返回45；当天未完成不会错误归零，次日12:00结算规则固定。
- [ ] 30个工作区日期含今日；未开始/未计划/漏记录三者区分，训练实际间隔unknown不伪装达标。
- [ ] 记录迟到仍计分子；记录率不是达标率；没打开的日期照算分母。
- [ ] 修改历史事实触发重算并能识别旧验收计算需重新生成；UI只展示Core结果。
- [ ] 颜色有文字补充，手机无横向滚动。

## 推荐执行环境 / 模型

Trae + GLM-5.3。凭据只在宿主安全存储，模型可用性如未验证先检查。

## 推荐 Review 环境 / 模型

Hermes + DeepSeek V4 Pro；WorkBuddy + Hy4 视觉复核。审查者拿diff、原始验证输出和本卡，不接受作者口头自证。高风险不可只做廉价初审。

## 风险

**Medium**。失败写证据并保持BLOCKED；修复在本任务闭环，不跳至下一任务。

## 完成回执与下一步

填写`docs/planning/evidence/MVP-004/result.md`：commit、变更文件、测试实际命令/原始输出、未验证项、Review人/模型/结论、回滚影响。更新task-index.json对应status前先有签核；格式按03-execution.md §3。

完成后才能进入：**MVP-005**。
