# P0-007｜注册health-only Agent并实际运行常驻Keepalive

状态：PLANNED（本轮仅交付任务说明，未施工）。

## 本卡最低上下文

项目目录：`/Users/wangxinkai/Documents/Morrow`。M1=Phase0+MVP；产品为单用户、多端同步、以三个作息锚点验证留存的静态生活工作台。后端固定Supabase，Human记录级LWW，Agent OCC，DB trigger原子version，所有写可审计；Agent不直连数据库。唯一业务Core在私有PostgreSQL语义函数，Web直接Human RPC，Edge仅Agent适配；M1业务Agent入口关闭，唯一health例外。前端原生HTML/CSS/JS，发行单HTML，默认GitHub Pages+本地fallback。不能引入Phase1功能。

Codex已冻结架构，在M1前不介入；不要把“等待Codex”作为工序。只读本卡指定章节与前置产物即可开工。若与原产品冲突，以原产品V2优先并记录具体冲突；未验证能力标待验证。任何当前合同变更须记录AD补充和独立Review，不能改掉安全或阶段门禁。

## 目标

注册health-only Agent并实际运行常驻Keepalive。

## 前置依赖

P0-006已独立Review为PASS；其证据必须实际可读。

## 输入

补充必读：02-contracts.md C-05.1（health管理参数、激活/撤销、锁序、幂等创建）。

01-architecture.md AD03/04/§4；02-contracts.md C04/06；03-execution.md §8；P0-004管理RPC与真实常驻宿主。 所有相对路径均相对docs/planning，源代码路径相对项目根。

## 施工范围

实现Supabase health Edge adapter与DB get_system_status事务；外部GET转内部POST/VOLATILE RPC以允许行锁和审计写；唯一scope system:health、无生活数据输出。注册client，宿主secret store保存独立token，handler默认拒绝。实际配置每日07:00 Asia/Shanghai调度，30s总deadline和有界退避，成功不刷屏。演练正常/错误/过期token、撤销、轮换，至少获得一次由真实调度启动的成功事件。记录未来7天频率证据，不能用睡眠中的开发机假冒常驻。

## 交付物

supabase/functions/health；宿主真实调度配置的脱敏副本；health审计与撤销/轮换证据；docs/operations/keepalive.md。

## 明确不做什么

业务Agent写、MCP安装、数据库内部cron自救、未经授权的外部消息渠道。

## 验收条件

- [ ] 返回health成功前实际查询DB并提交activity_log；只ping静态页面的实现不通过。
- [ ] 缺失/无效/超scope/撤销凭据均不读生活事实，错误不含token/SQL。
- [ ] token和hash不进前端/导出/通用response receipt，撤销旧token后失败新token成功。
- [ ] 至少一次定时触发成功且能对应activity request_id，调度配置每日执行；≥3次/周在M1补齐实际证据。
- [ ] 业务Agent路由不存在；health失败可诊断，Resume流程没有保证永不暂停的承诺。

## 推荐执行环境 / 模型

Hermes + Kimi K3。凭据只在宿主安全存储，模型可用性如未验证先检查。

## 推荐 Review 环境 / 模型

WorkBuddy + GLM-5.3；安全改动由Hermes不同于作者的GLM-5.3审查。审查者拿diff、原始验证输出和本卡，不接受作者口头自证。高风险不可只做廉价初审。

## 风险

**High**。失败写证据并保持BLOCKED；修复在本任务闭环，不跳至下一任务。

## 完成回执与下一步

填写`docs/planning/evidence/P0-007/result.md`：commit、变更文件、测试实际命令/原始输出、未验证项、Review人/模型/结论、回滚影响。更新task-index.json对应status前先有签核；格式按03-execution.md §3。

完成后才能进入：**P0-008**。
