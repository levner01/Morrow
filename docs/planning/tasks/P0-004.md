# P0-004｜实现唯一Core的身份、事务、版本、审计与幂等框架

状态：PLANNED（本轮仅交付任务说明，未施工）。

## 本卡最低上下文

项目目录：`/Users/wangxinkai/Documents/Morrow`。M1=Phase0+MVP；产品为单用户、多端同步、以三个作息锚点验证留存的静态生活工作台。后端固定Supabase，Human记录级LWW，Agent OCC，DB trigger原子version，所有写可审计；Agent不直连数据库。唯一业务Core在私有PostgreSQL语义函数，Web直接Human RPC，Edge仅Agent适配；M1业务Agent入口关闭，唯一health例外。前端原生HTML/CSS/JS，发行单HTML，默认GitHub Pages+本地fallback。不能引入Phase1功能。

Codex已冻结架构，在M1前不介入；不要把“等待Codex”作为工序。只读本卡指定章节与前置产物即可开工。若与原产品冲突，以原产品V2优先并记录具体冲突；未验证能力标待验证。任何当前合同变更须记录AD补充和独立Review，不能改掉安全或阶段门禁。

## 目标

实现唯一Core的身份、事务、版本、审计与幂等框架。

## 前置依赖

P0-003已独立Review为PASS；其证据必须实际可读。

## 输入

补充必读：02-contracts.md C-05.1（health管理参数、激活/撤销、锁序、幂等创建）。

01-architecture.md §3.10/4/5/10.1；02-contracts.md C04/05/10；P0-003 Schema与固定合同。 所有相对路径均相对docs/planning，源代码路径相对项目根。

## 施工范围

实现public invoker→private definer入口，固定search_path和具体EXECUTE，限制owner来源。实现version有效更新trigger、可信actor、业务变更审计、workspace revision、receipt命令框架与Human LWW/Agent OCC内部分支。按client→credential→state→receipt→resource锁序；初始化先锁预置state。业务拒绝返回envelope并留审计，技术故障原子回滚。实现health身份管理RPC，脚本本地产token只传hash，receipt不存原始token。尚无生活UI，通过测试专用不可公开入口验证事务。

## 交付物

新的Core migration；角色/RPC授权清单；token签发/轮换最小受限脚本；单事务及actor测试；evidence/P0-004/result.md。

## 明确不做什么

完整业务AgentHTTP/MCP、任意table/module/JSON写接口、将私有helper直接暴露给浏览器。

## 验收条件

- [ ] Human直DML失败、语义身份可识别；伪造owner/actor/source拒绝；service_role未进入前端。
- [ ] 有效更新原子增版/no-op不增；业务/audit/revision/receipt任何一步故障整体回滚。
- [ ] 同key重放不重写；同key不同input拒绝；Agent同expected仅一成功，create0不能覆盖已存记录。
- [ ] 撤销事务使用同锁序；Human初始化并发只建一次，不能对不存在state行自称锁定。
- [ ] 签发管理响应/receipt/日志无原始token；只读收据查询不执行未知请求。

## 推荐执行环境 / 模型

Trae + Kimi K3。凭据只在宿主安全存储，模型可用性如未验证先检查。

## 推荐 Review 环境 / 模型

Hermes + GLM-5.3 深审；WorkBuddy + DeepSeek V4.1 Flash 独立反例初审。审查者拿diff、原始验证输出和本卡，不接受作者口头自证。高风险不可只做廉价初审。

## 风险

**High**。失败写证据并保持BLOCKED；修复在本任务闭环，不跳至下一任务。

## 完成回执与下一步

填写`docs/planning/evidence/P0-004/result.md`：commit、变更文件、测试实际命令/原始输出、未验证项、Review人/模型/结论、回滚影响。更新task-index.json对应status前先有签核；格式按03-execution.md §3。

完成后才能进入：**P0-005**。
