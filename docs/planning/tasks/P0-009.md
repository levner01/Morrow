# P0-009｜Phase0门禁签核并交付MVP开工包

状态：PLANNED（本轮仅交付任务说明，未施工）。

## 本卡最低上下文

项目目录：`/Users/wangxinkai/Documents/Morrow`。M1=Phase0+MVP；产品为单用户、多端同步、以三个作息锚点验证留存的静态生活工作台。后端固定Supabase，Human记录级LWW，Agent OCC，DB trigger原子version，所有写可审计；Agent不直连数据库。唯一业务Core在私有PostgreSQL语义函数，Web直接Human RPC，Edge仅Agent适配；M1业务Agent入口关闭，唯一health例外。前端原生HTML/CSS/JS，发行单HTML，默认GitHub Pages+本地fallback。不能引入Phase1功能。

Codex已冻结架构，在M1前不介入；不要把“等待Codex”作为工序。只读本卡指定章节与前置产物即可开工。若与原产品冲突，以原产品V2优先并记录具体冲突；未验证能力标待验证。任何当前合同变更须记录AD补充和独立Review，不能改掉安全或阶段门禁。

## 目标

Phase0门禁签核并交付MVP开工包。

## 前置依赖

P0-008已独立Review为PASS；其证据必须实际可读。

## 输入

03-execution.md Gate-P0；所有P0任务收据；01/02合同；P0-008运行产物。 所有相对路径均相对docs/planning，源代码路径相对项目根。

## 施工范围

独立按Gate-P0逐项查证，核对contract/schema/RPC/发行版本一致性和未开放业务路由。形成Phase0结论与剩余风险；验收失败退回对应任务修复且原审核者重验，不请求Codex回场。核对health至少一次真实定时执行和后续计划；不额外等14天，也不声称已有一周运行。整理给MVP-001的精简输入。

## 交付物

evidence/P0-009/gate-report.md；phase0-baseline版本/commit；tasks状态更新；MVP开工所需合同/环境索引。

## 明确不做什么

生活功能施工、替代凯哥判定下一Phase成立、等待Codex批准。

## 验收条件

- [ ] Gate-P0所有必需项有真实证据，未验证项不标PASS。
- [ ] 高风险权限/并发已有异模型签核；未解决P0/P1为零。
- [ ] MVP-001能从索引获取固定Schema/RPC/fixtures和可用部署环境。
- [ ] Phase0→MVP没有误加14天门禁；Phase1仍LOCKED。

## 推荐执行环境 / 模型

Hermes + GLM-5.3。凭据只在宿主安全存储，模型可用性如未验证先检查。

## 推荐 Review 环境 / 模型

WorkBuddy + Kimi K3；凯哥仅补真实设备/账号缺口。审查者拿diff、原始验证输出和本卡，不接受作者口头自证。高风险不可只做廉价初审。

## 风险

**High**。失败写证据并保持BLOCKED；修复在本任务闭环，不跳至下一任务。

## 完成回执与下一步

填写`docs/planning/evidence/P0-009/result.md`：commit、变更文件、测试实际命令/原始输出、未验证项、Review人/模型/结论、回滚影响。更新task-index.json对应status前先有签核；格式按03-execution.md §3。

完成后才能进入：**MVP-001**。
