# MVP-005｜交付一致、全量、无凭据的JSON导出

状态：PLANNED（本轮仅交付任务说明，未施工）。

## 本卡最低上下文

项目目录：`/Users/wangxinkai/Documents/Morrow`。M1=Phase0+MVP；产品为单用户、多端同步、以三个作息锚点验证留存的静态生活工作台。后端固定Supabase，Human记录级LWW，Agent OCC，DB trigger原子version，所有写可审计；Agent不直连数据库。唯一业务Core在私有PostgreSQL语义函数，Web直接Human RPC，Edge仅Agent适配；M1业务Agent入口关闭，唯一health例外。前端原生HTML/CSS/JS，发行单HTML，默认GitHub Pages+本地fallback。不能引入Phase1功能。

Codex已冻结架构，在M1前不介入；不要把“等待Codex”作为工序。只读本卡指定章节与前置产物即可开工。若与原产品冲突，以原产品V2优先并记录具体冲突；未验证能力标待验证。任何当前合同变更须记录AD补充和独立Review，不能改掉安全或阶段门禁。

## 目标

交付一致、全量、无凭据的JSON导出。

## 前置依赖

MVP-004已独立Review为PASS；其证据必须实际可读。

## 输入

02-contracts.md C08/09/T11/12；01-architecture.md AD08/§3.9；已部署导出视图、owner RLS。 所有相对路径均相对docs/planning，源代码路径相对项目根。

## 施工范围

设置页导出按钮，客户端直接从Supabase固定列security_invoker视图读取六集合，BIGINT服务端转字符串。R0→keyset500分页→R1，有变更最多重试2次；全量含tombstone/settings，列白名单排除凭据/receipts。生成schema_version/exported_at/data/count/hash文件，RFC8785并用固定向量验证。构造1001行和10000日志非生产fixture做完整性/内存，浏览器实际下载并重新打开校验。

## 交付物

ExportService前端适配/校验脚本；脱敏fixture导出样本；计数/hash/引用/分页/secret scan证据；使用说明。

## 明确不做什么

真实导入数据库、远程备份平台、无限数据量承诺、秘密或完整个人数据提交git。

## 验收条件

- [ ] 文件命名和顶层字段符合合同；5必需表+workspace_settings全量，无前1000行截断。
- [ ] 版本9007199254740993准确往返，不能JS解析后才转string；data hash验证通过。
- [ ] 导出期间插入/更新/softdelete/health审计改变revision，混合包拒绝并重试。
- [ ] token、hash、session、service_role、credentials与receipt均不在文件；真实data只写受保护本地路径。
- [ ] 失败不下载半包；超过25MiB明确容量失败；完成离线round-trip和引用验证但不声称Restore已经实现。

## 推荐执行环境 / 模型

Trae + GLM-5.3。凭据只在宿主安全存储，模型可用性如未验证先检查。

## 推荐 Review 环境 / 模型

Hermes + DeepSeek V4 Pro；WorkBuddy + Kimi K3 安全复核。审查者拿diff、原始验证输出和本卡，不接受作者口头自证。高风险不可只做廉价初审。

## 风险

**High**。失败写证据并保持BLOCKED；修复在本任务闭环，不跳至下一任务。

## 完成回执与下一步

填写`docs/planning/evidence/MVP-005/result.md`：commit、变更文件、测试实际命令/原始输出、未验证项、Review人/模型/结论、回滚影响。更新task-index.json对应status前先有签核；格式按03-execution.md §3。

完成后才能进入：**MVP-006**。
