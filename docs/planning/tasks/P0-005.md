# P0-005｜用独立安全与并发测试封住核心风险

状态：PLANNED（本轮仅交付任务说明，未施工）。

## 本卡最低上下文

项目目录：`/Users/wangxinkai/Documents/Morrow`。M1=Phase0+MVP；产品为单用户、多端同步、以三个作息锚点验证留存的静态生活工作台。后端固定Supabase，Human记录级LWW，Agent OCC，DB trigger原子version，所有写可审计；Agent不直连数据库。唯一业务Core在私有PostgreSQL语义函数，Web直接Human RPC，Edge仅Agent适配；M1业务Agent入口关闭，唯一health例外。前端原生HTML/CSS/JS，发行单HTML，默认GitHub Pages+本地fallback。不能引入Phase1功能。

Codex已冻结架构，在M1前不介入；不要把“等待Codex”作为工序。只读本卡指定章节与前置产物即可开工。若与原产品冲突，以原产品V2优先并记录具体冲突；未验证能力标待验证。任何当前合同变更须记录AD补充和独立Review，不能改掉安全或阶段门禁。

## 目标

用独立安全与并发测试封住核心风险。

## 前置依赖

P0-004已独立Review为PASS；其证据必须实际可读。

## 输入

02-contracts.md C10的S01–S09/T01–T10；01-architecture.md §4/10；P0-003/004的diff与测试环境。 所有相对路径均相对docs/planning，源代码路径相对项目根。

## 施工范围

编写和执行真实PostgreSQL角色/并发/故障注入测试；此时health仅测已实现DB授权框架，HTTP真实反例留P0-007；覆盖anon、owner、非owner、private读/EXECUTE、伪造actor、direct DML、credential过期/撤销、幂等重放和唯一键。20独立连接同时有效UPDATE，另测2个Agent同expected。对所有definer、grants、搜索路径、角色继承和可调用函数做白名单审计。报告P0/P1缺陷并交原任务作者修复，复验后才推进。测试专用入口从生产权限中移除。

## 交付物

supabase/tests/及必要测试脚本；evidence/P0-005/security-matrix.md、concurrency-results、review.md；修复diff链接。

## 明确不做什么

只mock数据库的假并发测试、删掉失败用例、把临时绕过安全留进生产。

## 验收条件

- [ ] 至少20个有效更新返回不同连续版本，最终version等于初值+有效更新数。
- [ ] 两同expected只1成功；同key同body只一个业务变更；软删键不自动复活。
- [ ] anon读写无数据/无变更，owner与非owner真实身份测试；不是拿service_role跑完宣布RLS通过。
- [ ] 撤销并发保证撤销提交后无新授权写提交；审计技术失败无半事务。
- [ ] 内部Agent成功/重放投影按当前read scope裁剪，write-only及撤read后重放不泄漏payload；仍不开放生产业务Agent端点。
- [ ] 无未解决P0/P1安全缺陷；P0-004作者不可代替独立签核。

## 推荐执行环境 / 模型

Hermes + GLM-5.3。凭据只在宿主安全存储，模型可用性如未验证先检查。

## 推荐 Review 环境 / 模型

WorkBuddy + Kimi K3；原实现作者修复后Hermes重新验证。审查者拿diff、原始验证输出和本卡，不接受作者口头自证。高风险不可只做廉价初审。

## 风险

**High**。失败写证据并保持BLOCKED；修复在本任务闭环，不跳至下一任务。

## 完成回执与下一步

填写`docs/planning/evidence/P0-005/result.md`：commit、变更文件、测试实际命令/原始输出、未验证项、Review人/模型/结论、回滚影响。更新task-index.json对应status前先有签核；格式按03-execution.md §3。

完成后才能进入：**P0-006**。
