# P0-008｜完成双机访问和故障恢复的真实交付验证

状态：PLANNED（本轮仅交付任务说明，未施工）。

## 本卡最低上下文

项目目录：`/Users/wangxinkai/Documents/Morrow`。M1=Phase0+MVP；产品为单用户、多端同步、以三个作息锚点验证留存的静态生活工作台。后端固定Supabase，Human记录级LWW，Agent OCC，DB trigger原子version，所有写可审计；Agent不直连数据库。唯一业务Core在私有PostgreSQL语义函数，Web直接Human RPC，Edge仅Agent适配；M1业务Agent入口关闭，唯一health例外。前端原生HTML/CSS/JS，发行单HTML，默认GitHub Pages+本地fallback。不能引入Phase1功能。

Codex已冻结架构，在M1前不介入；不要把“等待Codex”作为工序。只读本卡指定章节与前置产物即可开工。若与原产品冲突，以原产品V2优先并记录具体冲突；未验证能力标待验证。任何当前合同变更须记录AD补充和独立Review，不能改掉安全或阶段门禁。

## 目标

完成双机访问和故障恢复的真实交付验证。

## 前置依赖

P0-007已独立Review为PASS；其证据必须实际可读。

## 输入

补充必读：02-contracts.md C-05.1（health管理参数、激活/撤销、锁序、幂等创建）。

P0-002网络矩阵；P0-006发行包；P0-007health；01-architecture.md失败矩阵；03-execution.md §8。 所有相对路径均相对docs/planning，源代码路径相对项目根。

## 施工范围

在获得具体发布授权后部署同源HTML到选定GitHub Pages，保存可离线取得的发行HTML。公司/家机各验证真实页面、邮箱密码登录、数据库读、已实现的Human manage_health_client_v1创建/停用专用临时health客户端并读取配置（不分发token、不重开generic测试入口）、断网报错、重新连接；本地双击路径单独验SDK/Auth/storage。如fileorigin实际不支持必要能力，用已批准loopback方案交付固定启动方法并标明原因。提供暂停识别和Dashboard Resume、丢失Session、下载草稿、版本回退runbook。

## 交付物

evidence/P0-008/device-matrix.md与脱敏截图/网络记录；docs/operations/deployment-recovery.md；Pages URL、HTML路径/hash。

## 明确不做什么

偷偷改全局代理、未经授权公开仓库/公网隧道、用公司机结果代表家机。

## 验收条件

- [ ] 公司机和家机均有一种真实可登录、读写、重试的稳定方式；curl200不能代替浏览器验收。
- [ ] 双击HTML被真实测试；若需loopback，明确不是双击成功，替代方式在两机实际验证。
- [ ] Supabase异常时静态壳不白屏、可恢复；不为测试主动暂停/破坏生产数据。
- [ ] 发布包能在Pages不可达前预先保存并在实际降级场景使用；安装依赖和启动步骤完整。
- [ ] 网络/浏览器/时间/hash证据齐全，缺家机证据保持BLOCKED。

## 推荐执行环境 / 模型

Trae + Kimi K2.7 Code。凭据只在宿主安全存储，模型可用性如未验证先检查。

## 推荐 Review 环境 / 模型

WorkBuddy + GLM-5.3。审查者拿diff、原始验证输出和本卡，不接受作者口头自证。高风险不可只做廉价初审。

## 风险

**Medium**。失败写证据并保持BLOCKED；修复在本任务闭环，不跳至下一任务。

## 完成回执与下一步

填写`docs/planning/evidence/P0-008/result.md`：commit、变更文件、测试实际命令/原始输出、未验证项、Review人/模型/结论、回滚影响。更新task-index.json对应status前先有签核；格式按03-execution.md §3。

完成后才能进入：**P0-009**。
