# P0-002｜验证真实Supabase、Auth、Edge及双机访问前提

状态：PLANNED（本轮仅交付任务说明，未施工）。

## 本卡最低上下文

项目目录：`/Users/wangxinkai/Documents/Morrow`。M1=Phase0+MVP；产品为单用户、多端同步、以三个作息锚点验证留存的静态生活工作台。后端固定Supabase，Human记录级LWW，Agent OCC，DB trigger原子version，所有写可审计；Agent不直连数据库。唯一业务Core在私有PostgreSQL语义函数，Web直接Human RPC，Edge仅Agent适配；M1业务Agent入口关闭，唯一health例外。前端原生HTML/CSS/JS，发行单HTML，默认GitHub Pages+本地fallback。不能引入Phase1功能。

Codex已冻结架构，在M1前不介入；不要把“等待Codex”作为工序。只读本卡指定章节与前置产物即可开工。若与原产品冲突，以原产品V2优先并记录具体冲突；未验证能力标待验证。任何当前合同变更须记录AD补充和独立Review，不能改掉安全或阶段门禁。

## 目标

验证真实Supabase、Auth、Edge及双机访问前提。

## 前置依赖

P0-001已独立Review为PASS；其证据必须实际可读。

## 输入

本任务卡；01-architecture.md AD01–04/§4/§11；03-execution.md §7/8；P0-001环境清单。 所有相对路径均相对docs/planning，源代码路径相对项目根。

## 施工范围

取得凯哥指定的免费Supabase项目或在明确实施授权下创建；登记project_ref、URL、唯一owner UUID，关闭公开注册。核验当前PG/CLI/SDK版本、pg_jsonschema和security_invoker视图支持。用隔离可删除探针验证Human邮箱密码登录、Data API、Edge custom token认证、真实DB只读调用。公司/家机对项目实际URL与静态候选入口做访问检查，家机若需要凯哥执行提供一条完整脚本/操作清单回传。探针不得公开生活数据。固定依赖版本而不是猜最新。

## 交付物

docs/runtime-environment.md实值（非敏感）；evidence/P0-002/network-matrix.md和探针输出；版本/扩展矩阵；阻塞与fallback结论。临时探针清理记录。

## 明确不做什么

完整产品数据库、业务Agent API、永久宽松策略、复制凭据到证据或命令行。

## 验收条件

- [ ] 项目级URL而非仅supabase.com域名连通；Human登录与只读请求成功，非owner身份拒绝路径有实证。
- [ ] Edge在公司机/家机/拟调度宿主分别记录成功或明确失败；自定义token缺失/错误被拒，verify_jwt配置与handler一致。
- [ ] pg_jsonschema用一个合法/非法payload验证；服务端int8::text精确返回9007199254740993。
- [ ] 记录file://Auth/storage/CORS原型结果；原型不等于最终fallback验收，最终在P0-008重验。
- [ ] 阻塞项采用架构批准的loopback/受信health宿主分支或保持BLOCKED，不擅换后端、不建立公网隧道。

## 推荐执行环境 / 模型

Trae + Kimi K3。凭据只在宿主安全存储，模型可用性如未验证先检查。

## 推荐 Review 环境 / 模型

Hermes + GLM-5.3。审查者拿diff、原始验证输出和本卡，不接受作者口头自证。高风险不可只做廉价初审。

## 风险

**High**。失败写证据并保持BLOCKED；修复在本任务闭环，不跳至下一任务。

## 完成回执与下一步

填写`docs/planning/evidence/P0-002/result.md`：commit、变更文件、测试实际命令/原始输出、未验证项、Review人/模型/结论、回滚影响。更新task-index.json对应status前先有签核；格式按03-execution.md §3。

完成后才能进入：**P0-003**。
