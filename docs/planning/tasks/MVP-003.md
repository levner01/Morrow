# MVP-003｜完成三态同步、持久草稿与未知结果处理

状态：PLANNED（本轮仅交付任务说明，未施工）。

## 本卡最低上下文

项目目录：`/Users/wangxinkai/Documents/Morrow`。M1=Phase0+MVP；产品为单用户、多端同步、以三个作息锚点验证留存的静态生活工作台。后端固定Supabase，Human记录级LWW，Agent OCC，DB trigger原子version，所有写可审计；Agent不直连数据库。唯一业务Core在私有PostgreSQL语义函数，Web直接Human RPC，Edge仅Agent适配；M1业务Agent入口关闭，唯一health例外。前端原生HTML/CSS/JS，发行单HTML，默认GitHub Pages+本地fallback。不能引入Phase1功能。

Codex已冻结架构，在M1前不介入；不要把“等待Codex”作为工序。只读本卡指定章节与前置产物即可开工。若与原产品冲突，以原产品V2优先并记录具体冲突；未验证能力标待验证。任何当前合同变更须记录AD补充和独立Review，不能改掉安全或阶段门禁。

## 目标

完成三态同步、持久草稿与未知结果处理。

## 前置依赖

MVP-002已独立Review为PASS；其证据必须实际可读。

## 输入

02-contracts.md C04/07/T01/02/05/10/13/14；01-architecture.md §6/9；MVP-002页面。 所有相对路径均相对docs/planning，源代码路径相对项目根。

## 施工范围

实现server snapshot/per-record command/form draft三类state，记录单flight队列、note600ms debounce、即时打卡。持久化草稿并检查失败；unknown先只读查收据/最新值、确认后同key重试，绝不自动覆盖。处理Session失效、恢复登录、旧response到达、focus/online刷新、logout草稿、不同owner隔离。file存储失败给下载草稿/loopback说明。

## 交付物

assets/js/{store,drafts,transport}；故障注入E2E；两个独立浏览器/账号设备同步证据。

## 明确不做什么

自动离线队列、字段merge、Realtime/Service Worker、客户端version+1。

## 验收条件

- [ ] 同步文案仅已同步/同步中/同步失败·重试；旧快照和本地草稿不冒充已同步。
- [ ] 断网→输入→确认草稿写盘→刷新浏览器→恢复草稿成功；storage拒绝时不谎报安全保存。
- [ ] A成功但response丢失、B再写、A同key重试：只返回原收据，UI刷新B版本；A从未提交时明确确认LWW风险再重试。
- [ ] 不同锚点无覆盖，同锚点新意图按DB提交LWW；Agent分支不因human参数绕OCC。
- [ ] 旧慢读不盖新写，刷新不覆盖未提交表单，跨owner不泄露草稿；online事件不自动写。

## 推荐执行环境 / 模型

Trae + Kimi K3。凭据只在宿主安全存储，模型可用性如未验证先检查。

## 推荐 Review 环境 / 模型

Hermes + GLM-5.3；WorkBuddy + DeepSeek V4.1 Flash 独立场景复核。审查者拿diff、原始验证输出和本卡，不接受作者口头自证。高风险不可只做廉价初审。

## 风险

**High**。失败写证据并保持BLOCKED；修复在本任务闭环，不跳至下一任务。

## 完成回执与下一步

填写`docs/planning/evidence/MVP-003/result.md`：commit、变更文件、测试实际命令/原始输出、未验证项、Review人/模型/结论、回滚影响。更新task-index.json对应status前先有签核；格式按03-execution.md §3。

完成后才能进入：**MVP-004**。
