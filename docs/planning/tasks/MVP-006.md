# MVP-006｜完成集成发布、七天真实使用和M1联合验收

状态：PLANNED（本轮仅交付任务说明，未施工）。

## 本卡最低上下文

项目目录：`/Users/wangxinkai/Documents/Morrow`。M1=Phase0+MVP；产品为单用户、多端同步、以三个作息锚点验证留存的静态生活工作台。后端固定Supabase，Human记录级LWW，Agent OCC，DB trigger原子version，所有写可审计；Agent不直连数据库。唯一业务Core在私有PostgreSQL语义函数，Web直接Human RPC，Edge仅Agent适配；M1业务Agent入口关闭，唯一health例外。前端原生HTML/CSS/JS，发行单HTML，默认GitHub Pages+本地fallback。不能引入Phase1功能。

Codex已冻结架构，在M1前不介入；不要把“等待Codex”作为工序。只读本卡指定章节与前置产物即可开工。若与原产品冲突，以原产品V2优先并记录具体冲突；未验证能力标待验证。任何当前合同变更须记录AD补充和独立Review，不能改掉安全或阶段门禁。

## 目标

完成集成发布、七天真实使用和M1联合验收。

## 前置依赖

MVP-005已独立Review为PASS；其证据必须实际可读。

## 输入

03-execution.md Gate-M1/Gate-Phase1/§9；所有任务PASS证据；04-acceptance.md；当前release。 所有相对路径均相对docs/planning，源代码路径相对项目根。

## 施工范围

按真实发布物重新构建与回归关键链路，获得发布授权后更新Pages并交付同hash本地HTML。运行最终双机互写/刷新、断网恢复、手机无溢出、导出、权限回归。建立连续7天真实使用记录和health7天频率证据，明确开始/结束/版本。期间只修M1缺陷，重大中断重新起连续窗口；不能用fixture填天数。完成联合签核和M1返回包；停在Phase1门禁。

## 交付物

dist发行HTML/hash；docs/planning/evidence/M1-HANDOFF.md；7天真实表与记录率计算；最终验收矩阵；缺陷清单与回滚runbook。

## 明确不做什么

为凑验收批量写假生活数据、上线Phase1、让Codex回场成为M1前置依赖、隐藏未完成的真实设备或天数验收。

## 验收条件

- [ ] Gate-M1所有产品/安全/故障/真实使用项有证据；7天实际使用不是测试数据或一句“待后续验证”。
- [ ] A→B及B→A真实设备同步；手机320px和实际手机检查；最终HTML文件可打开且fallback可用。
- [ ] health滚动7天≥3次成功；所有高风险任务异模型签核，P0/P1缺陷为零。
- [ ] 状态为M1_COMPLETE/PHASE1_LOCKED；若7天未满只能ENGINEERING_READY/USAGE_PENDING，不得标M1完成。
- [ ] Phase1须另等14天、≥80%及凯哥本人明确确认；无人代签，不等Codex才能完成M1。

## 推荐执行环境 / 模型

Hermes + Kimi K3 组织验证；Trae + 原任务模型修复。凭据只在宿主安全存储，模型可用性如未验证先检查。

## 推荐 Review 环境 / 模型

Hermes + GLM-5.3 主审；WorkBuddy + Hy4 UI见证；凯哥确认真实使用事实。审查者拿diff、原始验证输出和本卡，不接受作者口头自证。高风险不可只做廉价初审。

## 风险

**High**。失败写证据并保持BLOCKED；修复在本任务闭环，不跳至下一任务。

## 完成回执与下一步

填写`docs/planning/evidence/MVP-006/result.md`：commit、变更文件、测试实际命令/原始输出、未验证项、Review人/模型/结论、回滚影响。更新task-index.json对应status前先有签核；格式按03-execution.md §3。

完成后才能进入：**M1_COMPLETE；Phase1仍LOCKED**。
