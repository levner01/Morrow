# P0-003｜落地版本化Schema与共享合同

状态：PLANNED（本轮仅交付任务说明，未施工）。

## 本卡最低上下文

项目目录：`/Users/wangxinkai/Documents/Morrow`。M1=Phase0+MVP；产品为单用户、多端同步、以三个作息锚点验证留存的静态生活工作台。后端固定Supabase，Human记录级LWW，Agent OCC，DB trigger原子version，所有写可审计；Agent不直连数据库。唯一业务Core在私有PostgreSQL语义函数，Web直接Human RPC，Edge仅Agent适配；M1业务Agent入口关闭，唯一health例外。前端原生HTML/CSS/JS，发行单HTML，默认GitHub Pages+本地fallback。不能引入Phase1功能。

Codex已冻结架构，在M1前不介入；不要把“等待Codex”作为工序。只读本卡指定章节与前置产物即可开工。若与原产品冲突，以原产品V2优先并记录具体冲突；未验证能力标待验证。任何当前合同变更须记录AD补充和独立Review，不能改掉安全或阶段门禁。

## 目标

落地版本化Schema与共享合同。

## 前置依赖

P0-002已独立Review为PASS；其证据必须实际可读。

## 输入

01-architecture.md §3/4；02-contracts.md C01/02/04/05（含C05.1）/06/08/09；P0-002验证过的PG/扩展环境。 所有相对路径均相对docs/planning，源代码路径相对项目根。

## 施工范围

编写首批正式migration：life_data/settings/state、owner singleton、clients、private credentials/receipts/payload_schemas、activity_log、封闭advice/rules；字段与FK/CHECK/INDEX按蓝图。预置owner的workspace_state零值，初始化settings另走业务命令。生成严格payload/命令/结果/导出JSON Schema及fixtures；C06的Phase2部分只生成协议合同与投影样例，不创建业务端点，migration中固化schema/hash。所有表RLS开启，默认权限拒绝；此任务不为了演示临时放行写。建立security_invoker导出视图，所有BIGINT在SQL转text。

## 交付物

supabase/migrations/首批顺序文件；contracts/v1/{payloads,commands,results,fixtures}；schema映射和hash校验；数据库结构测试证据。

## 明确不做什么

完整锚点业务、通用CRUD API、Postgres ENUM模块列表、全部JSON的GIN索引；不改已经应用的migration。

## 验收条件

- [ ] 隔离空库可一次应用，重建结果一致；原文要求的基础表与必需私有表齐全。
- [ ] (owner,module,key)唯一、softdelete仍占键、不可变列和payload_v规则有约束；schema拒绝未知/非法字段。
- [ ] anon/private默认权限封闭；owner客户端不能读取credentials/hash。
- [ ] 导出视图遵从RLS；超过2^53的version实际返回准确字符串。
- [ ] advice/rules无可写业务入口，未来模块只留contract边界；schema变更未提前实现功能。

## 推荐执行环境 / 模型

Trae + Kimi K3。凭据只在宿主安全存储，模型可用性如未验证先检查。

## 推荐 Review 环境 / 模型

Hermes + GLM-5.3。审查者拿diff、原始验证输出和本卡，不接受作者口头自证。高风险不可只做廉价初审。

## 风险

**High**。失败写证据并保持BLOCKED；修复在本任务闭环，不跳至下一任务。

## 完成回执与下一步

填写`docs/planning/evidence/P0-003/result.md`：commit、变更文件、测试实际命令/原始输出、未验证项、Review人/模型/结论、回滚影响。更新task-index.json对应status前先有签核；格式按03-execution.md §3。

完成后才能进入：**P0-004**。
