# P0-001｜建立可持续施工的仓库与环境基线

状态：PLANNED（本轮仅交付任务说明，未施工）。

## 本卡最低上下文

项目目录：`/Users/wangxinkai/Documents/Morrow`。M1=Phase0+MVP；产品为单用户、多端同步、以三个作息锚点验证留存的静态生活工作台。后端固定Supabase，Human记录级LWW，Agent OCC，DB trigger原子version，所有写可审计；Agent不直连数据库。唯一业务Core在私有PostgreSQL语义函数，Web直接Human RPC，Edge仅Agent适配；M1业务Agent入口关闭，唯一health例外。前端原生HTML/CSS/JS，发行单HTML，默认GitHub Pages+本地fallback。不能引入Phase1功能。

Codex已冻结架构，在M1前不介入；不要把“等待Codex”作为工序。只读本卡指定章节与前置产物即可开工。若与原产品冲突，以原产品V2优先并记录具体冲突；未验证能力标待验证。任何当前合同变更须记录AD补充和独立Review，不能改掉安全或阶段门禁。

## 目标

建立可持续施工的仓库与环境基线。

## 前置依赖

本架构交接包已读取；当前无实现基线，先检查现场。

## 输入

本任务卡；03-execution.md §1–3/7；01-architecture.md §2。 所有相对路径均相对docs/planning，源代码路径相对项目根。

## 施工范围

检查目录/现有git/远端/本机工具，保护两份输入文档和本施工包。无git时初始化本地仓库，建立忽略规则（secret、真实导出、临时草稿、node_modules）；不创建远端或推送。按架构准备必要源目录、最小package.json和任务状态表，不创建未来空工程。填写非敏感runtime-environment模板和待补输入；定义任务收据及分支/提交规则。

## 交付物

根README和AGENTS按本包补充；.gitignore；package.json；docs/runtime-environment.md；docs/planning/evidence/P0-001/result.md；本任务可审查diff/commit。

## 明确不做什么

产品UI、业务代码、migration、未来MCP/PWA工程；不安装一大套框架。

## 验收条件

- [ ] 两份输入文档内容hash未变，规划文件保留；现有仓库内容无破坏。
- [ ] 环境清单明确已安装/未安装/待验证；不把工程包的gh、代理历史写成当前事实。
- [ ] git忽略覆盖秘密和真实个人导出；无原始token/密码入文件；无未经授权远端副作用。
- [ ] 状态表15任务均PLANNED，P0-001完成须review签核后标PASS。

## 推荐执行环境 / 模型

Trae + GLM-5.3 Flash。凭据只在宿主安全存储，模型可用性如未验证先检查。

## 推荐 Review 环境 / 模型

WorkBuddy + DeepSeek V4.1 Flash。审查者拿diff、原始验证输出和本卡，不接受作者口头自证。高风险不可只做廉价初审。

## 风险

**Low**。失败写证据并保持BLOCKED；修复在本任务闭环，不跳至下一任务。

## 完成回执与下一步

填写`docs/planning/evidence/P0-001/result.md`：commit、变更文件、测试实际命令/原始输出、未验证项、Review人/模型/结论、回滚影响。更新task-index.json对应status前先有签核；格式按03-execution.md §3。

完成后才能进入：**P0-002**。
